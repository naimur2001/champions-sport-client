import React from 'react';

const SectionTitle = ({title}) => {
  return (
    <div>
      <h1 className='text-2xl text-gray-600 font-semibold font-mono text-center my-1'>{title}</h1>
    </div>
  );
};

export default SectionTitle;